[package]
name = "afinador_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "afinador_engine"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
